# 03_Strategies

Short cross-reference notes linking hypotheses to the code folders under
`../../strategies/`. The actual Python code is NOT stored here; it lives
in `strategies/Hxxxx_<slug>/`. This folder only holds markdown
descriptors.

Naming: `Sxxxx_<slug>.md` where xxxx matches the hypothesis id.
