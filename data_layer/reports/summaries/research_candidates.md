# Research Candidates

Last refresh: 2026-05-04 15:11 UTC.
Cells that pass every stability gate at once. Tier T uses `p <= 0.05`; Tier M uses `p <= 0.10` (relaxed while only 365 days of data are available). All rows already require `n >= 80` and walk-forward sign stability for the matching tier. Source: `walk_forward.md` + `permutation_test.md`.

## Cross-symbol Pareto + stability (highest grade)

None at the current window. The auditor cross-symbol Pareto gate is currently empty for both tiers.

## Tier T single-symbol candidates (`p <= 0.05`)

None.

## Tier M single-symbol candidates (`p <= 0.10`)

None.

